1. Users (Auth Module)Central identity table for all platform users.Column NameData TypeConstraints & KeysDescriptionidUUIDPrimary KeyUnique identifier for the user.emailVARCHAR(255)Unique, Not NullUsed for login.password_hashVARCHAR(255)Not NullBcrypt hashed password.roleVARCHAR(50)Not Null'STUDENT', 'RECRUITER', or 'ADMIN'.created_atTIMESTAMPDefault NOW()Account creation timestamp.updated_atTIMESTAMPDefault NOW()Last update timestamp.is_bannedBOOLEANDefault FALSEWhether the user account has been banned by an admin.
2. Companies (Jobs/Recruiter Module)Organizations that recruiters belong to.Column NameData TypeConstraints & KeysDescriptionidUUIDPrimary KeyUnique identifier for the company.nameVARCHAR(255)Not NullCompany name.websiteVARCHAR(255)NullableCompany website URL.logo_urlVARCHAR(512)NullableURL to company logo (S3/Cloudinary).created_atTIMESTAMPDefault NOW()Record creation timestamp.
3. Recruiters (Recruiter Module)Profile data specific to recruiter accounts.Column NameData TypeConstraints & KeysDescriptionidUUIDPrimary KeyUnique identifier for the recruiter profile.user_idUUIDForeign Key (users.id), Unique1:1 relationship with Auth user.company_idUUIDForeign Key (companies.id)Links recruiter to a company.first_nameVARCHAR(100)Not NullRecruiter's first name.last_nameVARCHAR(100)Not NullRecruiter's last name.is_approvedBOOLEANDefault FALSEAdmin approval status for safety.
4. Students (Student Module)Profile data for job seekers.Column NameData TypeConstraints & KeysDescriptionidUUIDPrimary KeyUnique identifier for the student profile.user_idUUIDForeign Key (users.id), Unique1:1 relationship with Auth user.first_nameVARCHAR(100)Not NullStudent's first name.last_nameVARCHAR(100)Not NullStudent's last name.universityVARCHAR(255)NullableName of current/past college.graduation_yearINTEGERNullableExpected or actual graduation year.degreeVARCHAR(100)NullableE.g., "B.Tech Computer Science".skillsTEXT[]NullableArray of strings (e.g., ['React', 'Node']).github_urlVARCHAR(255)NullableLink to GitHub profile.linkedin_urlVARCHAR(255)NullableLink to LinkedIn profile.
5. Resumes (Student Module)Stores PDF links and parsed text. A student can upload multiple over time, but use one primarily.Column NameData TypeConstraints & KeysDescriptionidUUIDPrimary KeyUnique identifier for the resume.student_idUUIDForeign Key (students.id)The student who owns this resume.file_urlVARCHAR(512)Not NullCloud URL of the uploaded PDF.parsed_textTEXTNullableRaw text extracted from the PDF.is_primaryBOOLEANDefault TRUEIndicates the default resume for quick-apply.created_atTIMESTAMPDefault NOW()Upload timestamp.
6. AI Resume Analyses (AI Module)Stores the results from the Gemini API so we don't have to re-process the same resume twice (saves API costs).Column NameData TypeConstraints & KeysDescriptionidUUIDPrimary KeyUnique identifier for the analysis.resume_idUUIDForeign Key (resumes.id), Unique1:1 relationship with a specific resume.statusVARCHAR(20)Default 'PROCESSING''PROCESSING', 'COMPLETED', or 'FAILED'.scoreINTEGERNullableATS score out of 100. Null while processing or if analysis failed.missing_skillsTEXT[]NullableArray of skills missing from the resume.formatting_tipsTEXT[]NullableArray of actionable tips generated by AI.error_messageTEXTNullableError details if the analysis failed (e.g., unparseable PDF).created_atTIMESTAMPDefault NOW()When the analysis was generated.
7. Jobs (Jobs Module)Job postings created by recruiters.Column NameData TypeConstraints & KeysDescriptionidUUIDPrimary KeyUnique identifier for the job post.recruiter_idUUIDForeign Key (recruiters.id)Recruiter who posted the job.company_idUUIDForeign Key (companies.id)Redundant but optimizes read queries.titleVARCHAR(255)Not NullE.g., "Junior Frontend Developer".descriptionTEXTNot NullFull job description.required_skillsTEXT[]Not NullUsed for matching against student skills.employment_typeVARCHAR(50)Not Null'INTERNSHIP' or 'FULL_TIME'.statusVARCHAR(50)Default 'PENDING''PENDING', 'ACTIVE', or 'REJECTED'.created_atTIMESTAMPDefault NOW()Post creation date.updated_atTIMESTAMPDefault NOW()Last update timestamp.
8. Applications (Applications Module)The transactional link between a student, their resume, and a job. A unique constraint on (job_id, student_id) prevents duplicate applications.Column NameData TypeConstraints & KeysDescriptionidUUIDPrimary KeyUnique identifier for the application.job_idUUIDForeign Key (jobs.id), Unique(job_id, student_id)The job being applied to.student_idUUIDForeign Key (students.id), Unique(job_id, student_id)The applicant.resume_idUUIDForeign Key (resumes.id)Snapshot of the resume used for this app.statusVARCHAR(50)Default 'APPLIED''APPLIED', 'SHORTLISTED', or 'REJECTED'.applied_atTIMESTAMPDefault NOW()Date of application.updated_atTIMESTAMPDefault NOW()Last status update timestamp.